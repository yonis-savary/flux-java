# Purpose of this directory

Angular [does not support env files](https://angular.dev/tools/cli/environments), instead of having 
multiple configuration/envs, or havhing to split back/front envs

Those two files are used by the Dockerfile and enriched with values from root .env